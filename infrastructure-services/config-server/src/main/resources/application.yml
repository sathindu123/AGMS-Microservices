server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      config:
      server:
        git:
          uri: file:///C:/Users/Visun/Desktop/agms-config-repo
          search-paths: '{application}'

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

